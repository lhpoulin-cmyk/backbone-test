# Optional path hints under each worker path. If these tier directories exist,
# they are preferred. Otherwise the runbook samples readable files under path.
SMALL_DIR=small
MEDIUM_DIR=medium
LARGE_DIR=large

